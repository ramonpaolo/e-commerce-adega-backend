import express from "express"
import HomeControllerShow from "../controllers/homeController";

const app = express()

// app.get("/", async (req, res) => res.send(await HomeControllerShow.getData())  )

export default app;